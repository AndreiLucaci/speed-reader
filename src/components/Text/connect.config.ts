import { setText, setWPM, setReader } from "../../state/Reader/actions";

export const mapDispatchToProps = {
  setText,
  setWPM,
  setReader
};

export default {
  mapStateToProps: {},
  mapDispatchToProps
};
